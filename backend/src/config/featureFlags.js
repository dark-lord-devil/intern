// E-Faws Feature Flag Configuration
// Used for development control, debugging, and rollout simulations.

const featureFlags = {
  enable_ai: true,
  enable_mock_market: true,
  enable_admin_controls: true
};

module.exports = featureFlags;
